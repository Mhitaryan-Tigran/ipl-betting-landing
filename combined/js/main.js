/**
 * Main initialization
 * Initializes all components on DOMContentLoaded
 */
document.addEventListener('DOMContentLoaded', function() {
  initIplSlider();
  initMatchSlider();
  initReviewsSlider();
  initAccordion();
  initMobileMenu();
});
